header.header 
    nav.nav.nav--tours
        a.nav__el(href="/") All tours
    div.header__logo
                img(src="/img/logo-white.png" alt="Natours logo")
    nav.nav.nav--user
        if user 
            a(href="/logout" class="nav__el nav__el--logout") Log Out
            a(href="/me" class="nav__el") 
                img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
                span= user.name.split(' ')[0]
        else
            a.nav__el.nav__el--cta(href='/login') Log in
            a.nav__el.nav__el--cta(href='#') Sign up


            //- if user kiểm tra xem có tồn tại dữ liệu user trong res.locals không
            //- a(href="/me" class="nav__el") trỏ về route /me trong viewsController
